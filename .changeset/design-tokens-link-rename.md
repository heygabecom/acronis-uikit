---
'@acronis-platform/design-tokens': minor
---

Rename `colors.text.onBrand/onStatus/onSurface.link` to `*.link-idle` (removing the ambiguous bare `link` alias). Update all component tokens that referenced the old paths (Button, ButtonMenu, CardFilter, Chip, Link).
