name: Test - CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PNPM
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Cache node modules
        id: cache-pnpm
        uses: actions/cache@v4
        env:
          cache-name: cache-node-modules
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-${{ env.cache-name }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-${{ env.cache-name }}-
            ${{ runner.os }}-

      # --frozen-lockfile: refuse the install if pnpm-lock.yaml is out of
      # sync with package.json. This is what guarantees CI sees the exact
      # tree a contributor saw locally.
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm -r lint

      # Typecheck is scoped to the published library for now — apps still
      # have pre-existing source-level type errors (TanStack Form types in
      # apps/demos, base-ui RadioGroup onValueChange, a missing sonner
      # declaration in apps/docs) that aren't fixed by the catalog. Widen
      # this to \`pnpm -r typecheck\` once those are sorted.
      - name: Typecheck UI library
        run: pnpm --filter @acronis-platform/shadcn-uikit typecheck

      # Build runs last so the topological build order (ui → demo / docs)
      # is exercised against the same lockfile we just installed from.
      - name: Build
        run: pnpm -r build

      - name: Test
        run: pnpm -r test
